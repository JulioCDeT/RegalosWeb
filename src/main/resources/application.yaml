server:
  port: 8080
  servlet:
    context-path: /v1/regalos-navideños

spring:
  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3306/tienda_regalos
    username: root
    password: devPassword